{
  "name": "rollup-plugin-sass-variables",
  "version": "0.1.0",
  "description": "load sass/scss variables as javascript object",
  "main": "dist/rollup-plugin-sass-variables.cjs.js",
  "module": "dist/rollup-plugin-sass-variables.es.js",
  "repository": "https://github.com/gmfun/rollup-plugin-sass-variables",
  "scripts": {
    "clean": "rm -rf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && rollup -c",
    "build:watch": "npm run clean && rollup -c -w",
    "prepublishOnly": "npm run build"
  },
  "author": "gmfun",
  "license": "MIT"
}
